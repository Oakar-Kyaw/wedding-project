import React from 'react'
import ReactDOM from 'react-dom/client'
import RouteFile from './RouteFile.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RouteFile />
  </React.StrictMode>,
)
